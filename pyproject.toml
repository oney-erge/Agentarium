[project]
name = "agentarium"
version = "0.1.0"
description = "Visual AI physics sandbox where agents build objects in simulated worlds"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.141.1",
    "uvicorn[standard]>=0.53.0",
    "pydantic>=2.13.5",
    "pymunk>=6.9",
    "httpx>=0.27",
    "python-dotenv>=1.0",
    "pyyaml>=6.0",
    "aiosqlite>=0.20",
]

[project.scripts]
agentarium = "agentarium.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["backend/agentarium"]

[tool.ruff]
line-length = 100
src = ["backend"]
exclude = ["backend/agentarium/static"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
ignore = ["E501"]

[tool.pytest.ini_options]
testpaths = ["backend/tests"]
pythonpath = ["backend"]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "pytest-asyncio>=0.24",
    "ruff>=0.16.7",
    "httpx>=0.27",
    # Browser UI diagnosis: after installing, run: python -m playwright install chromium
    "playwright>=1.62.0",
]
